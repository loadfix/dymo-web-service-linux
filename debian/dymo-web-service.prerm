#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        if [ -d /run/systemd/system ]; then
            systemctl stop dymo-web-service.service >/dev/null 2>&1 || true
        fi
        ;;
esac

#DEBHELPER#
exit 0
